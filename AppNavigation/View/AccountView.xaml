<UserControl x:Class="AppNavigation.View.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppNavigation.View" xmlns:Components="clr-namespace:AppNavigation.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Components:NavigationBar DataContext="{Binding NavigationBarViewModel}"/>

        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Left">
                <Label Content="Welcome to Account" FontSize="20" Margin="20"/>
                <TextBlock Text="{Binding Username}"/>
                <TextBlock Text="{Binding Email}"/>
                <Button Content="Home" Command="{Binding NavigateToHome}" Margin="20"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
